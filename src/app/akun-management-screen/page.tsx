import React from 'react';
import AppShell from '@/components/AppShell';
import AkunContent from './components/AkunContent';

export default function AkunPage() {
  return (
    <AppShell>
      <AkunContent />
    </AppShell>
  );
}